<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ini_apa"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ini_apa"
        android:supportsRtl="true"
        android:theme="@style/Theme.RepeatIt"
        tools:targetApi="31">

        <activity android:name=".LoginActivity">
            <!-- Bisa menambahkan konfigurasi lain jika perlu -->
        </activity>

        <activity android:name=".SignUpActivity">
            <!-- Anda bisa menambahkan intent-filter di sini jika diperlukan -->
        </activity>

        <activity android:name=".AfterSignUpActivity">
            <!-- Anda bisa menambahkan intent-filter di sini jika diperlukan -->
        </activity>

        <activity android:name=".AdminAddSongActivity">
            <!-- Anda bisa menambahkan intent-filter di sini jika diperlukan -->
        </activity>

        <activity android:name=".RepeatItMainActivity">
            <!-- Anda bisa menambahkan intent-filter di sini jika diperlukan -->
        </activity>

        <activity android:name=".SongMainActivity">
            <!-- Anda bisa menambahkan intent-filter di sini jika diperlukan -->
        </activity>


        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>